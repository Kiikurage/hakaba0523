extends layout

block content
	include header.jade
	
	.container
		.row
			.col-xs-12
				video.video_video(src="/api/video/#{video.videoId}", controls)
				h1.video_title #{video.title}
				p by #{video.user.name}
